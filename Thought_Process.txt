→ → → → → → → → → → → → → → → React Use state ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ← ←
In REACT JS the use state is hook, Build In Function in React Js that allows functional components to manage state.
It is used to control the behaviour of our website components.
        It also allows you to make attractive UI components.


    So, that we learn about its use and its functions.
